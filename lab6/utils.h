#pragma once
#include <stdio.h>

int logOfTwo(int);
int mask(int);
char isHex(int);
int hexValue(char);
int decValue(char);
char* allocateString(const char*);
int skipLine(FILE*);
int skipBlanks(FILE*);

